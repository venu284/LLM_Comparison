{
  "task_id": "TS-012",
  "category": "typescript",
  "difficulty": "hard",
  "title": "Deep Partial and Deep Readonly",
  "prompt": "You are a web developer. Complete the following task.\n\nTASK: Define and export `DeepPartial<T>`, `DeepReadonly<T>`, and `DeepRequired<T>` for nested object structures. Export `deepMerge<T>(target: T, source: DeepPartial<T>): T` that recursively merges source into target while skipping `undefined` values.\n\nCONSTRAINTS:\n- Use recursive TypeScript types\n- Export all required types and functions\n- Compile under `tsc --strict --noEmit`\n- Do not use `any` except where explicitly required by the prompt constraint\n\nIMPORTANT:\n- Return ONLY the code. No explanations.\n- Output a single complete file.\n- Do not use markdown code blocks.",
  "constraints": [
    "Use recursive TypeScript types",
    "Export all required types and functions",
    "Compile under `tsc --strict --noEmit`",
    "Do not use `any` except where explicitly required by the prompt constraint"
  ],
  "expected_output": {
    "type": "single_file",
    "extension": ".ts",
    "export_type": "named"
  },
  "test_file": "tests/typescript/TS-012.test.ts",
  "test_count": 8,
  "tags": ["typescript", "recursive-types", "deep-merge"],
  "reference_solution": "solutions/typescript/TS-012.ts"
}

